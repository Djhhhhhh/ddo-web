# task-05 · 整体索引 design/DESIGN.md

## 目标

将原根目录 `DESIGN.md` 重构为规范目录的整体索引文件 `design/DESIGN.md`：设计系统总览 + 分层加载说明 + 文件地图 + 迭代守则 + 裁决与迁移记录。索引不放具体规则。

## 输入

- 原文：仓库根 `DESIGN.md`（§1 主题氛围、§2.8 主题映射、§10.3 迭代守则）
- 上游产物：task-01 ~ task-04 产出的 18 份规则文件（文件地图须与实际文件一一对应）
- plan DEC-2（索引结构）、DEC-5（裁决留痕）、DEC-6（沉淀规则单行指针）

## 文件清单

| 文件 | 说明 |
|---|---|
| `design/DESIGN.md` | 整体索引（分层加载中间层） |

## 关键要点

- 结构（DEC-2）：① 设计哲学与主题摘要（源自 §1，修正后的绿信号叙述）；② 分层加载说明（AGENTS.md → 本文件 → 规则文件）；③ 文件地图表：`| 文件 | 管辖范围 | 关键 token |`，覆盖 9 组件 + 9 基础共 18 行，文件名与链接逐一可解析；④ 迭代守则（原 §10.3 六条）+ 一行沉淀规则指针（「样式变更沉淀流程见 ../../AGENTS.md 开发流程节」，DEC-6）；⑤ 裁决与迁移记录：§1–§10 → 新文件的映射表 + 裁决项（`text-tertiary` 笔误、绿信号、Fira Code）及依据。
- 相对链接一律相对 `design/` 目录（如 `components/button.md`）。
- 索引本身不记录裸色值（裁决记录中引用笔误对照值除外，须注明为裁决证据）。
- §10.2 五个示例 prompt 的归属表（标题区→button、代码块→cards-and-panels、命令栏→inputs-and-forms、表格→dense-table、Tabs→tabs）记入文件地图的备注或迁移记录。

## 关联验收点

- G1（文件落位）、G2（导航与链接）、G5（沉淀指针）

## 完成标准

- 文件地图覆盖全部 18 份规则文件且相对链接可解析；含迭代守则与裁决迁移记录；`grep -q '沉淀' design/DESIGN.md` 命中。
