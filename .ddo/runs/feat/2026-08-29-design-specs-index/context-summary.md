# 上下文摘要

> 自动生成自项目上下文扫描。

## 已加载来源

| 文件路径 | 摘要 |
|---|---|
| CLAUDE.md | 仓库指引：两条权威来源（DESIGN.md + src/styles/tokens.css）；铁律：禁裸 hex、代码块恒暗、函数组件+命名导出、CSS Modules、图标仅用 Icon.tsx 内联 SVG；Node 22（nvm exec 22）命令；完成标准 = lint + build 必须通过。 |
| DESIGN.md | 设计系统唯一权威（445 行，未入 git）：暗色优先双主题；中性蓝灰 + 单一绿色信号 accent #10B981；Manrope/Fira Code 字重 400/500/600；§4 组件样式（按钮/标签/表单/导航/卡片/Tabs/表格/Toast/滚动条）；8px 间距、6/8/12 圆角、e0–e3 阴影；图标 1.6px 内联 SVG；Do/Don't；响应式；§10 token 速查与迭代守则。 |

## 上下文缺失

- AGENTS.md（项目根不存在；CLAUDE.md 承担同角色，已加载）
